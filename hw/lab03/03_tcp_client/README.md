# tcp-client

## 📝 Условие

В аргументах командной строки задаются:

- имя хоста `HOST`;
- имя сервиса (номер порта) `SERVICE`;
- строковый параметр `KEY`.

Напишите программу `solution03_03.c`, которая:

- подключается к хосту `HOST` и сервису `SERVICE` по протоколу **TCP**;
- отправляет серверу строку `KEY`;
- получает от сервера целое число `K > 0`;
- отправляет серверу числа от `0` до `K` включительно;
- получает от сервера целое **беззнаковое 64-битное** число;
- выводит полученное число на стандартный поток вывода и завершает работу.

Обмен данными между клиентом и сервером ведётся в **текстовом виде**.
После каждой операции вывода необходимо выводить разделитель и **сбрасывать буфер**.

При разрыве соединения клиент должен завершить работу с кодом завершения `0`.

## Требования

- использовать протокол **TCP**;
- для разрешения имени хоста использовать `getaddrinfo`;
- для обмена данными разрешается использовать высокоуровневый ввод-вывод
  (`fprintf`, `fscanf`, `fflush`);
- при получении конца файла или ошибке ввода-вывода клиент должен
  корректно завершить работу с кодом `0`.

## ℹ️ Примечание

- Формат передаваемых данных — текстовый.
- Порядок обмена данными должен строго соответствовать описанному в условии.

## ⚙️ Быстрая сборка и тесты

```bash
cd hw/lab03/03_tcp_client
make
make test
```

## 🧪 Примеры запуска

```bash
./solution03_03 localhost 12345 hello
```

Вывод программы:

```
<число, полученное от сервера>
```

## 🐳 Работа в Docker

### 1. Сборка Docker-образа

```bash
docker build -t dev-intel .
```

### 2. Запуск контейнера

```bash
docker run -it --rm -v "$(pwd)":/workspace -w /workspace dev-intel
```

### 3. Сборка и тестирование внутри контейнера

```bash
cd hw/lab03/03_tcp_client
make
make test
```

### 4. Завершение работы контейнера

```bash
exit
```
или нажмите `Ctrl + D`.

## 🧹 Очистка

```bash
make clean
```

## 🚀 Автотесты в GitHub Actions

```bash
git add .
git commit -m "lab03: tcp-client"
git push   # статус проверки появится в Actions ✅
```