# mp-hello

## 📝 Условие

В аргументах командной строки задаётся одно целое число `N`.

Напишите программу `solution03_04.py`, которая:

- создаёт `N` дочерних процессов с использованием `multiprocessing.Process`;
- если аргумент командной строки не задан, считать `N = 5`;
- каждый процесс вызывает одну и ту же функцию `f(name, wait)`, где:
    - `name` — номер процесса (от `0` до `N - 1`);
    - `wait` — случайное целое число от `1` до `4` включительно;
- внутри функции процесс:
    - выводит на стандартный поток вывода значения `name` и `wait`;
    - приостанавливает выполнение на `wait` секунд;
    - выводит строку:
      ```
      hello <name> <wait>
      ```

Родительский процесс должен запустить все дочерние процессы
и дождаться завершения каждого из них.

Порядок строк в выводе не фиксируется.

## Требования

- использовать только стандартную библиотеку Python;
- использовать одну и ту же функцию для всех создаваемых процессов;
- количество создаваемых процессов должно соответствовать значению `N`.

## ℹ️ Примечание

- Вывод разных процессов может быть перемешан.
- Задержка выполнения должна осуществляться с помощью `time.sleep`.

## ⚙️ Быстрая проверка и тесты

```bash
cd hw/lab03/04_mp_hello
make
make test
```

## 🧪 Примеры запуска

```bash
./solution03_04.py 3
```

Вывод программы содержит строки вида:

```
0 <wait>
hello 0 <wait>
1 <wait>
hello 1 <wait>
2 <wait>
hello 2 <wait>
```

Порядок строк произвольный.

## 🐳 Работа в Docker

### 1. Сборка Docker-образа

```bash
docker build -t dev-intel .
```

### 2. Запуск контейнера

```bash
docker run -it --rm -v "$(pwd)":/workspace -w /workspace dev-intel
```

### 3. Запуск внутри контейнера

```bash
cd hw/lab03/04_mp_hello
make test
```

### 4. Завершение работы контейнера

```bash
exit
```
или нажмите `Ctrl + D`.

## 🧹 Очистка

```bash
make clean
```

## 🚀 Автотесты в GitHub Actions

```bash
git add .
git commit -m "lab03: mp-hello"
git push   # статус проверки появится в Actions ✅
```