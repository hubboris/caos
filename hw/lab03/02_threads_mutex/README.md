# threads-mutex

## 📝 Условие

Напишите программу `solution03_02.c`, которая:

- создаёт **3 нити**, пронумерованные от `0` до `2`;
- нити работают с общим **глобальным массивом** типа `double` из 3 элементов;
- каждая нить выполняет **1000000** итераций следующих операций:

Операции нитей:

- нить `0`: прибавляет к `a[0]` число `100` и вычитает из `a[1]` число `101`;
- нить `1`: прибавляет к `a[1]` число `200` и вычитает из `a[2]` число `201`;
- нить `2`: прибавляет к `a[2]` число `300` и вычитает из `a[0]` число `301`.

После завершения всех нитей программа печатает значения массива по одному числу на строку
в формате `%.10g`.

Требования:

- использовать **один мьютекс** для обеспечения корректной работы;
- сравнить результат работы с результатом работы **без** использования мьютексов;
- использовать **одну** функцию нити для всех трёх нитей;
- объявить константу со значением `3` и константу со значением `1000000` и использовать их.

## ℹ️ Примечание

- Для демонстрации различия «с мьютексом / без мьютекса» допускается сделать два режима сборки:
  обычный режим — с мьютексом, и режим без мьютекса (например, через `-DNO_MUTEX`).
- При корректной синхронизации результат должен быть детерминированным.

## ⚙️ Быстрая сборка и тесты

```bash
cd hw/lab03/02_threads_mutex   # перейти в папку задачи
make                            # сборка программы
make test                       # автотесты (unittest)
```

## 🧪 Примеры ввода и вывода

Ожидаемый результат в режиме **с мьютексом**:

```
-2.01e+08
9.9e+07
9.9e+07
```

(Формат вывода `%.10g` допускает эквивалентные представления этих чисел.)

## 🐳 Работа в Docker

### 1. Сборка Docker-образа

```bash
docker build -t dev-intel .
```

### 2. Запуск контейнера

```bash
docker run -it --rm -v "$(pwd)":/workspace -w /workspace dev-intel
```

### 3. Сборка и тестирование внутри контейнера

```bash
cd hw/lab03/02_threads_mutex
make
make test
```

### 4. Завершение работы контейнера

```bash
exit
```
или нажмите `Ctrl + D`.

## 🧹 Очистка

```bash
make clean
```

## 🚀 Автотесты в GitHub Actions

```bash
git add .
git commit -m "lab03: threads-mutex"
git push   # статус проверки появится в Actions ✅
```